[phases.install]
cmds = ["npm install", "npm --prefix client install"]

[phases.build]
cmds = ["npm run client:build", "npx prisma generate", "npx prisma migrate deploy", "node prisma/seed.js"]

[start]
cmd = "npm start"
